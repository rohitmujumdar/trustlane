"""trustgate (placeholder name) — real-time trust scoring for agent actions.

The core idea: before an agent takes an action, score *that action* on a few
legible signals. The score decides what 1Password releases — a full scoped
credential, a narrowed short-lived one, a human approval, or nothing at all.

Rename the package when you pick a real name (see trustgate/config.py).
"""

__version__ = "0.1.0"
