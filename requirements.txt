# TrustLane — Python dependencies
# Install: pip install -r requirements.txt

# Web framework (mock Expedia API server)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Data validation (used by FastAPI models)
pydantic>=2.6.0

# 1Password Python SDK
# Official SDK: https://github.com/1Password/onepassword-sdk-python
onepassword-sdk>=0.1.0

# Async HTTP (used by agent_loop when calling mock_expedia)
httpx>=0.27.0

# Environment variable loading
python-dotenv>=1.0.0

# Anthropic SDK (for LLM-powered agent — requires LIVE_LLM=1 and ANTHROPIC_API_KEY)
anthropic>=0.40.0

# Testing
pytest>=7.0
